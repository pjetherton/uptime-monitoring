/* Copyright (c) 2014 Paul Etherton
 */

Meteor.methods({
    setMobile: function(userId, mobile) {
	if (!Meteor.user() || !/^[+]\d{3,}$/.test(mobile)) {
	    return false;
	}
	Meteor.users.update(userId, {$set: {mobile: mobile}});
	return true;
    });
});
